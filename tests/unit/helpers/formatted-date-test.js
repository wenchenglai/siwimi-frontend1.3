import {
  formattedDate
} from 'xiwami-parents/helpers/formatted-date';

module('FormattedDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formattedDate(42);
  ok(result);
});
