import {
  stringText
} from '../../../helpers/string-text';
import { module, test } from 'qunit';

module('StringTextHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = stringText(42);
  assert.ok(result);
});
