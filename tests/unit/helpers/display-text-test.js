import {
  displayText
} from 'xiwami-parents/helpers/display-text';

module('DisplayTextHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = displayText(42);
  ok(result);
});
