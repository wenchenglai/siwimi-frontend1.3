import {
  shortenString
} from 'xiwami-parents/helpers/shorten-string';

module('ShortenStringHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = shortenString(42);
  ok(result);
});
