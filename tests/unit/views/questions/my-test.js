import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:questions/my', 'QuestionsMyView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
