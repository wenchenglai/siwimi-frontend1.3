import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:file-upload-view', 'FileUploadViewView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
