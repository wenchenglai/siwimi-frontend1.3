import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:radio-button', 'RadioButtonView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
