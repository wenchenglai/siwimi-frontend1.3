import Ember from 'ember';

export default Ember.Mixin.create({
    allTypes: ["Birthdays", "Concerts", "Farm Activity", "Library & Book Reading", "Misc", "Movies", "Museums", "Play Dates", "Festival", "Shows", "Sports"]
});
